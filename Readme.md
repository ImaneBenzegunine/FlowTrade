
### Setup Instructions


1. Create virtual environment:
   ```bash
   python -m venv venv
   ```

2. Activate virtual environment:
   - Windows:
     ```bash
     venv\Scripts\activate
     ```

3. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

```
 pip install "fastapi[standard]"
uvicorn main:app --reload


```

folder : AI

Notebooks created in: /mnt/data/deepseek_notebooks

Files produced:
- deepseek_sample.csv 
- 1_analysis_and_report.ipynb  (EDA + PDF export example)
- eda_report.pdf  (will be generated by running the analysis notebook)
- 2_cleaning.ipynb  (cleaning steps + cleaned CSV output deepseek_cleaned.csv)
- 3_ml_and_strategies.ipynb  (feature engineering, baseline ML, SMA strategy backtest)
- rf_example_model.joblib  (will be generated by running the ML notebook)

To run them in Google Colab:
1. Upload the files to Colab (Files panel) or mount your Google Drive and move them there.
2. Run cells in order. Some cells contain pip install lines you can uncomment if dependencies are missing.
